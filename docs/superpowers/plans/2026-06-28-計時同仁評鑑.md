# 計時同仁評鑑系統 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立計時同仁「態度 30 分」評鑑網頁 App：同仁匿名互評 → 自動算態度分 → 主管調整 → 查時薪，並預留職能 70 分擴充。

**Architecture:** GitHub Pages 靜態前端 ＋ Google Apps Script Web App 後端 ＋ Google Sheet 資料庫。**計分邏輯只有一份**（`js/scoring.js`，純函式、用 Node 內建測試器 TDD）；前端用它顯示，需要時把算好的列寫回 `結果` 分頁。Apps Script 只負責持久化與讀取（不重算分數，避免兩份邏輯分歧）。

**Tech Stack:** 原生 HTML/CSS/JS（ES Modules，無打包步驟）、Google Apps Script（`.gs`）、Node 18+ 內建 `node:test`（零 npm 依賴）。

## Global Constraints

- 前端為純靜態檔，**不得內嵌任何金鑰或通行碼**；通行碼一律由 Apps Script 端比對。
- 計分邏輯**只能有一份實作**：`js/scoring.js`。Apps Script 不重算分數。
- 評分項目固定 6 項、順序固定，由 `設定` 提供；計分函式以「長度 6 的數字陣列」處理，順序即項目順序。
- 態度分滿分 30（6 項 × 5）；職能分滿分 70；第一階段職能分留空。
- 互評匿名：無評分者欄位；防重靠 localStorage ＋ 裝置指紋雜湊。
- 所有金額/分數顯示四捨五入到小數 1 位（`round1`），計算過程保留完整精度。
- 語言：所有使用者可見文字用繁體中文。
- 不新增 npm 依賴；測試用 `node --test`。

### 跨任務共用資料型別（所有任務以此為準）

```
// 設定（doGet?action=config 回傳；不含通行碼）
Config = {
  quarter: string,                       // "2026-Q1"
  ratees: string[],                      // ["許雅筑", ...]
  items: { key: string, label: string, levels: string[] }[],  // 長度 6，levels 為 [5星,4星,3星,2星,1星] 描述
  wageTable: { min: number, max: number, wage: number }[]
}

// 互評送出 payload（doPost）
PeerSubmission = {
  type: "peer",
  quarter: string,
  fingerprint: string,
  note: string,
  ratings: { ratee: string, scores: number[] }[]   // 每位受評者一筆，scores 長度 6，值 1..5
}

// 主管調整 payload（doPost，需通行碼）
AdjustSubmission = {
  type: "adjust",
  passcode: string,
  quarter: string,
  ratee: string,
  attitudeAdjust: number,                // 可負
  attitudeReason: string,
  competencyAdjust: number,              // 可負
  competencyReason: string
}

// 管理頁資料（doGet?action=adminData，需通行碼）
AdminData = {
  config: Config,
  peerRatings: { ratee: string, scores: number[] }[],   // 已攤平的所有互評逐筆（該季）
  adjustments: { ratee: string, attitudeAdjust: number, competencyAdjust: number,
                 attitudeReason: string, competencyReason: string }[]
}

// scoring.aggregateRatee 回傳
RateeResult = {
  ratee: string,
  responseCount: number,
  itemAverages: number[] | null,         // 長度 6 或 null（無互評）
  attitude: number | null,               // 0..30 或 null
  attitudeAdjust: number,
  competency: number | null,             // 第一階段為 null
  competencyAdjust: number,
  subtotal: number | null,               // (attitude+attitudeAdjust) + (competency+competencyAdjust)，competency null 時只計態度
  competencyCounted: boolean,
  wage: { status: "ok", wage: number } | { status: "insufficient" } | { status: "manual" }
}
```

### 檔案結構

```
績效評核系統/
├─ index.html              # 同仁互評頁（公開匿名）
├─ admin.html              # 主管評分 + 結果/管理頁（通行碼）
├─ css/style.css
├─ js/
│  ├─ config.js            # APPS_SCRIPT_URL 常數（部署後填入）
│  ├─ scoring.js           # 純計分函式（唯一計分來源，Node 測試）
│  ├─ fingerprint.js       # 裝置指紋雜湊
│  ├─ validate.js          # 互評送出前驗證（純函式）
│  ├─ api.js               # 呼叫 Apps Script 的 fetch 封裝
│  ├─ eval-page.js         # 互評頁 UI 邏輯
│  └─ admin-page.js        # 管理頁 UI 邏輯
├─ apps-script/Code.gs     # 後端（doGet/doPost）
├─ tests/
│  ├─ scoring.test.js
│  ├─ validate.test.js
│  └─ fingerprint.test.js
├─ docs/superpowers/...    # 設計與計畫文件
└─ README.md               # 部署與設定步驟
```

---

## Phase A — 核心計分（純函式，Node TDD）

### Task 1: 專案 scaffold ＋ `averageItemScores`

**Files:**
- Create: `js/scoring.js`
- Create: `tests/scoring.test.js`
- Create: `package.json`

**Interfaces:**
- Produces: `averageItemScores(scoresList: number[][]): number[] | null` — 輸入同一位受評者的多筆互評（每筆長度 6），輸出每項平均（長度 6）；無任何互評回 `null`。

- [ ] **Step 1: 建立 package.json（讓 Node 用 ESM、定義 test 指令）**

```json
{
  "name": "mala-staff-eval",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test"
  }
}
```

- [ ] **Step 2: 寫失敗測試**

`tests/scoring.test.js`：
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { averageItemScores } from '../js/scoring.js';

test('averageItemScores 計算每項平均', () => {
  const list = [
    [5, 4, 3, 2, 1, 5],
    [3, 4, 3, 4, 1, 5],
  ];
  assert.deepEqual(averageItemScores(list), [4, 4, 3, 3, 1, 5]);
});

test('averageItemScores 無互評回 null', () => {
  assert.equal(averageItemScores([]), null);
});
```

- [ ] **Step 3: 執行測試確認失敗**

Run: `node --test`
Expected: FAIL（`averageItemScores` is not a function / 找不到模組匯出）

- [ ] **Step 4: 最小實作**

`js/scoring.js`：
```js
export function averageItemScores(scoresList) {
  if (!scoresList || scoresList.length === 0) return null;
  const n = scoresList[0].length;
  const sums = new Array(n).fill(0);
  for (const scores of scoresList) {
    for (let i = 0; i < n; i++) sums[i] += scores[i];
  }
  return sums.map((s) => s / scoresList.length);
}
```

- [ ] **Step 5: 執行測試確認通過**

Run: `node --test`
Expected: PASS（2 tests）

- [ ] **Step 6: Commit**

```bash
git add package.json js/scoring.js tests/scoring.test.js
git commit -m "feat: averageItemScores 計算互評各項平均"
```

---

### Task 2: `attitudeScore` ＋ `round1`

**Files:**
- Modify: `js/scoring.js`
- Modify: `tests/scoring.test.js`

**Interfaces:**
- Consumes: `averageItemScores`
- Produces:
  - `attitudeScore(itemAverages: number[] | null): number | null` — 6 項平均加總（0..30）；輸入 null 回 null。
  - `round1(n: number): number` — 四捨五入到小數 1 位。

- [ ] **Step 1: 寫失敗測試（加到 tests/scoring.test.js 末端）**

```js
import { attitudeScore, round1 } from '../js/scoring.js';

test('attitudeScore 為六項平均加總', () => {
  assert.equal(attitudeScore([4, 4, 3, 3, 1, 5]), 20);
});

test('attitudeScore 輸入 null 回 null', () => {
  assert.equal(attitudeScore(null), null);
});

test('round1 四捨五入到一位小數', () => {
  assert.equal(round1(3.456), 3.5);
  assert.equal(round1(20), 20);
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `node --test`
Expected: FAIL（`attitudeScore` / `round1` 未匯出）

- [ ] **Step 3: 實作（加到 js/scoring.js）**

```js
export function attitudeScore(itemAverages) {
  if (itemAverages === null) return null;
  return itemAverages.reduce((a, b) => a + b, 0);
}

export function round1(n) {
  return Math.round(n * 10) / 10;
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `node --test`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add js/scoring.js tests/scoring.test.js
git commit -m "feat: attitudeScore 與 round1"
```

---

### Task 3: `finalSubtotal`（含調整與職能留空處理）

**Files:**
- Modify: `js/scoring.js`
- Modify: `tests/scoring.test.js`

**Interfaces:**
- Produces: `finalSubtotal({ attitude, attitudeAdjust = 0, competency = null, competencyAdjust = 0 }): { subtotal: number | null, competencyCounted: boolean }`
  - `attitude` 為 null → `subtotal` null、`competencyCounted` false。
  - `competency` 為 null（第一階段）→ 只計 `attitude + attitudeAdjust`、`competencyCounted` false。
  - `competency` 為數字 → `(attitude+attitudeAdjust) + (competency+competencyAdjust)`、`competencyCounted` true。

- [ ] **Step 1: 寫失敗測試**

```js
import { finalSubtotal } from '../js/scoring.js';

test('finalSubtotal 第一階段只計態度（職能 null）', () => {
  assert.deepEqual(
    finalSubtotal({ attitude: 24, attitudeAdjust: 2 }),
    { subtotal: 26, competencyCounted: false }
  );
});

test('finalSubtotal 含職能時兩者相加', () => {
  assert.deepEqual(
    finalSubtotal({ attitude: 24, attitudeAdjust: -1, competency: 60, competencyAdjust: 5 }),
    { subtotal: 88, competencyCounted: true }
  );
});

test('finalSubtotal 態度 null 時 subtotal null', () => {
  assert.deepEqual(
    finalSubtotal({ attitude: null }),
    { subtotal: null, competencyCounted: false }
  );
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `node --test`
Expected: FAIL（`finalSubtotal` 未匯出）

- [ ] **Step 3: 實作**

```js
export function finalSubtotal({ attitude, attitudeAdjust = 0, competency = null, competencyAdjust = 0 }) {
  if (attitude === null || attitude === undefined) {
    return { subtotal: null, competencyCounted: false };
  }
  const attitudePart = attitude + attitudeAdjust;
  if (competency === null || competency === undefined) {
    return { subtotal: attitudePart, competencyCounted: false };
  }
  return { subtotal: attitudePart + competency + competencyAdjust, competencyCounted: true };
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `node --test`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add js/scoring.js tests/scoring.test.js
git commit -m "feat: finalSubtotal 計算最終小計與職能計入旗標"
```

---

### Task 4: `lookupWage`（區間查表 ＋ 邊界）

**Files:**
- Modify: `js/scoring.js`
- Modify: `tests/scoring.test.js`

**Interfaces:**
- Produces: `lookupWage(total: number | null, wageTable): { status: "ok", wage } | { status: "insufficient" } | { status: "manual" }`
  - `total` null → `{ status: "insufficient" }`（資料不足）。
  - 落在某 band（`min <= total <= max`）→ `{ status: "ok", wage }`。
  - 表外 → `{ status: "manual" }`（需人工確認）。

- [ ] **Step 1: 寫失敗測試**

```js
import { lookupWage } from '../js/scoring.js';

const wageTable = [
  { min: 90, max: 100, wage: 200 },
  { min: 80, max: 89, wage: 190 },
  { min: 70, max: 79, wage: 183 },
];

test('lookupWage 命中區間', () => {
  assert.deepEqual(lookupWage(85, wageTable), { status: 'ok', wage: 190 });
  assert.deepEqual(lookupWage(90, wageTable), { status: 'ok', wage: 200 });
});

test('lookupWage 資料不足', () => {
  assert.deepEqual(lookupWage(null, wageTable), { status: 'insufficient' });
});

test('lookupWage 表外需人工確認', () => {
  assert.deepEqual(lookupWage(50, wageTable), { status: 'manual' });
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `node --test`
Expected: FAIL（`lookupWage` 未匯出）

- [ ] **Step 3: 實作**

```js
export function lookupWage(total, wageTable) {
  if (total === null || total === undefined) return { status: 'insufficient' };
  for (const band of wageTable) {
    if (total >= band.min && total <= band.max) return { status: 'ok', wage: band.wage };
  }
  return { status: 'manual' };
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `node --test`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add js/scoring.js tests/scoring.test.js
git commit -m "feat: lookupWage 區間查表與邊界處理"
```

---

### Task 5: `aggregateRatee`（組合成一位同仁的完整結果）

**Files:**
- Modify: `js/scoring.js`
- Modify: `tests/scoring.test.js`

**Interfaces:**
- Consumes: `averageItemScores`, `attitudeScore`, `finalSubtotal`, `lookupWage`
- Produces: `aggregateRatee({ ratee, scoresList, adjustment, wageTable }): RateeResult`
  - `scoresList: number[][]`（該同仁所有互評）。
  - `adjustment: { attitudeAdjust?, competencyAdjust?, competency?, attitudeReason?, competencyReason? }`（可為空物件）。
  - 回傳 `RateeResult`（見 Global Constraints 型別）。

- [ ] **Step 1: 寫失敗測試**

```js
import { aggregateRatee } from '../js/scoring.js';

const wt = [
  { min: 25, max: 30, wage: 200 },
  { min: 20, max: 24, wage: 190 },
];

test('aggregateRatee 有互評＋態度調整', () => {
  const r = aggregateRatee({
    ratee: '許雅筑',
    scoresList: [[5, 5, 4, 4, 4, 4], [5, 5, 4, 4, 4, 4]],
    adjustment: { attitudeAdjust: 1 },
    wageTable: wt,
  });
  assert.equal(r.ratee, '許雅筑');
  assert.equal(r.responseCount, 2);
  assert.equal(r.attitude, 26);          // 5+5+4+4+4+4
  assert.equal(r.attitudeAdjust, 1);
  assert.equal(r.competency, null);
  assert.equal(r.subtotal, 27);          // 26 + 1
  assert.equal(r.competencyCounted, false);
  assert.deepEqual(r.wage, { status: 'manual' }); // 27 在 25..30→200? 27<=30 → ok
});

test('aggregateRatee 無互評→資料不足', () => {
  const r = aggregateRatee({ ratee: '王禹婕', scoresList: [], adjustment: {}, wageTable: wt });
  assert.equal(r.attitude, null);
  assert.equal(r.subtotal, null);
  assert.equal(r.responseCount, 0);
  assert.deepEqual(r.wage, { status: 'insufficient' });
});
```

> 註：第一個測試的 `wage` 預期應為 `{ status: 'ok', wage: 200 }`（27 落在 25..30）。實作完成後把該行預期改為 `{ status: 'ok', wage: 200 }` 再跑；此處保留是提醒實作者驗證查表方向。

- [ ] **Step 2: 執行測試確認失敗**

Run: `node --test`
Expected: FAIL（`aggregateRatee` 未匯出）

- [ ] **Step 3: 實作**

```js
export function aggregateRatee({ ratee, scoresList, adjustment = {}, wageTable }) {
  const itemAverages = averageItemScores(scoresList);
  const attitude = attitudeScore(itemAverages);
  const attitudeAdjust = adjustment.attitudeAdjust ?? 0;
  const competency = adjustment.competency ?? null;
  const competencyAdjust = adjustment.competencyAdjust ?? 0;
  const { subtotal, competencyCounted } = finalSubtotal({
    attitude, attitudeAdjust, competency, competencyAdjust,
  });
  return {
    ratee,
    responseCount: scoresList ? scoresList.length : 0,
    itemAverages,
    attitude,
    attitudeAdjust,
    competency,
    competencyAdjust,
    subtotal,
    competencyCounted,
    wage: lookupWage(subtotal, wageTable),
  };
}
```

- [ ] **Step 4: 修正測試預期並執行確認通過**

把第一個測試的 `wage` 預期改為 `{ status: 'ok', wage: 200 }`。
Run: `node --test`
Expected: PASS（全部 scoring 測試）

- [ ] **Step 5: Commit**

```bash
git add js/scoring.js tests/scoring.test.js
git commit -m "feat: aggregateRatee 組合單一同仁完整評鑑結果"
```

---

## Phase B — 前端純輔助（Node TDD）

### Task 6: `validatePeerSubmission`（互評送出前驗證）

**Files:**
- Create: `js/validate.js`
- Create: `tests/validate.test.js`

**Interfaces:**
- Produces: `validatePeerSubmission(ratings, ratees, itemCount): string[]` — 回傳錯誤訊息陣列；空陣列代表通過。規則：每位 `ratees` 都要有一筆；每筆 `scores` 長度 = `itemCount`、值皆為 1..5 整數。

- [ ] **Step 1: 寫失敗測試**

`tests/validate.test.js`：
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { validatePeerSubmission } from '../js/validate.js';

const ratees = ['A', 'B'];

test('全部填妥→無錯誤', () => {
  const ratings = [
    { ratee: 'A', scores: [5, 4, 3, 2, 1, 5] },
    { ratee: 'B', scores: [1, 2, 3, 4, 5, 1] },
  ];
  assert.deepEqual(validatePeerSubmission(ratings, ratees, 6), []);
});

test('缺一位受評者→報錯', () => {
  const ratings = [{ ratee: 'A', scores: [5, 4, 3, 2, 1, 5] }];
  const errs = validatePeerSubmission(ratings, ratees, 6);
  assert.ok(errs.some((e) => e.includes('B')));
});

test('分數超出範圍→報錯', () => {
  const ratings = [
    { ratee: 'A', scores: [6, 4, 3, 2, 1, 5] },
    { ratee: 'B', scores: [1, 2, 3, 4, 5, 1] },
  ];
  const errs = validatePeerSubmission(ratings, ratees, 6);
  assert.ok(errs.length > 0);
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `node --test tests/validate.test.js`
Expected: FAIL

- [ ] **Step 3: 實作**

`js/validate.js`：
```js
export function validatePeerSubmission(ratings, ratees, itemCount) {
  const errors = [];
  const byRatee = new Map((ratings || []).map((r) => [r.ratee, r]));
  for (const ratee of ratees) {
    const r = byRatee.get(ratee);
    if (!r) {
      errors.push(`尚未幫「${ratee}」評分`);
      continue;
    }
    if (!Array.isArray(r.scores) || r.scores.length !== itemCount) {
      errors.push(`「${ratee}」的評分項目不完整`);
      continue;
    }
    for (const s of r.scores) {
      if (!Number.isInteger(s) || s < 1 || s > 5) {
        errors.push(`「${ratee}」有項目尚未打分或分數無效`);
        break;
      }
    }
  }
  return errors;
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `node --test tests/validate.test.js`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add js/validate.js tests/validate.test.js
git commit -m "feat: validatePeerSubmission 互評送出前驗證"
```

---

### Task 7: `deviceFingerprint`（裝置指紋雜湊）

**Files:**
- Create: `js/fingerprint.js`
- Create: `tests/fingerprint.test.js`

**Interfaces:**
- Produces:
  - `hashString(input: string): string` — 穩定的 32-bit 十六進位雜湊（純函式，可測）。
  - `collectFingerprintSource(nav, screenObj): string` — 從 navigator/screen 組出特徵字串（純函式，可測）。
  - `deviceFingerprint(): string` — 瀏覽器端便利函式：`hashString(collectFingerprintSource(navigator, screen))`（瀏覽器執行，不寫單元測試）。

- [ ] **Step 1: 寫失敗測試**

`tests/fingerprint.test.js`：
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { hashString, collectFingerprintSource } from '../js/fingerprint.js';

test('hashString 對相同輸入穩定', () => {
  assert.equal(hashString('abc'), hashString('abc'));
});

test('hashString 對不同輸入不同', () => {
  assert.notEqual(hashString('abc'), hashString('abd'));
});

test('collectFingerprintSource 組出特徵字串', () => {
  const nav = { userAgent: 'UA', language: 'zh-TW', platform: 'MacIntel', hardwareConcurrency: 8 };
  const scr = { width: 1440, height: 900, colorDepth: 24 };
  const s = collectFingerprintSource(nav, scr);
  assert.ok(s.includes('UA') && s.includes('1440') && s.includes('zh-TW'));
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `node --test tests/fingerprint.test.js`
Expected: FAIL

- [ ] **Step 3: 實作**

`js/fingerprint.js`：
```js
export function hashString(input) {
  let h = 2166136261;                       // FNV-1a 32-bit
  for (let i = 0; i < input.length; i++) {
    h ^= input.charCodeAt(i);
    h = Math.imul(h, 16777619);
  }
  return (h >>> 0).toString(16).padStart(8, '0');
}

export function collectFingerprintSource(nav, screenObj) {
  return [
    nav.userAgent,
    nav.language,
    nav.platform,
    nav.hardwareConcurrency,
    screenObj.width,
    screenObj.height,
    screenObj.colorDepth,
    new Date().getTimezoneOffset(),
  ].join('|');
}

export function deviceFingerprint() {
  return hashString(collectFingerprintSource(navigator, screen));
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `node --test`
Expected: PASS（全部測試：scoring + validate + fingerprint）

- [ ] **Step 5: Commit**

```bash
git add js/fingerprint.js tests/fingerprint.test.js
git commit -m "feat: deviceFingerprint 裝置指紋雜湊"
```

---

## Phase C — Apps Script 後端

> Apps Script 無法用 Node 測試；每個任務以「部署後手動呼叫驗證」收尾。先在 Google Sheet 建好分頁，再貼上 `Code.gs`，部署為 Web App（執行身分：我自己；存取：任何人）。

### Task 8: Google Sheet 與 `Code.gs` 骨架 ＋ `doGet?action=config`

**Files:**
- Create: `apps-script/Code.gs`
- Create: `README.md`（部署步驟，後續任務補充）

**Interfaces:**
- Produces: HTTP `GET {WEBAPP_URL}?action=config` → JSON `Config`（不含通行碼）。

- [ ] **Step 1: 手動建立 Google Sheet 分頁（依設計文件 §3）**

建立 4 個分頁與標題列：
- `設定`：A 欄放鍵、B 欄起放值。列：`季度`、`通行碼`、`受評同仁`（B 起一格一名）、`時薪對照表`（每列 `分下限｜分上限｜時薪`，放在固定區塊 `WAGE_START` 列）、`項目`（每列 `key｜label｜5星｜4星｜3星｜2星｜1星`，6 列）。
- `評分紀錄`：`時間戳｜季度｜受評者｜效率｜互動｜團隊｜紀律｜學習｜出勤｜備註｜指紋`
- `主管調整`：`季度｜受評者｜態度調整｜態度原因｜職能調整｜職能原因｜時間｜操作者`
- `結果`：`季度｜受評者｜態度分｜態度調整｜職能分｜職能調整｜最終總分｜對應時薪｜互評份數`

> 為避免「設定」分頁解析過於脆弱，採固定命名範圍：在 Sheet 用「資料 → 命名範圍」建立 `CFG_quarter`、`CFG_passcode`、`CFG_ratees`、`CFG_items`(6×7)、`CFG_wage`(N×3)。`Code.gs` 只讀命名範圍。

- [ ] **Step 2: 寫 `Code.gs` 骨架與 config 讀取**

`apps-script/Code.gs`：
```js
function jsonOut(obj) {
  return ContentService.createTextOutput(JSON.stringify(obj))
    .setMimeType(ContentService.MimeType.JSON);
}

function ss() { return SpreadsheetApp.getActiveSpreadsheet(); }
function rng(name) { return ss().getRangeByName(name); }

function readConfig() {
  const quarter = rng('CFG_quarter').getValue();
  const ratees = rng('CFG_ratees').getValues().flat().filter(String);
  const itemsRaw = rng('CFG_items').getValues().filter((r) => r[0]);
  const items = itemsRaw.map((r) => ({ key: r[0], label: r[1], levels: r.slice(2, 7) }));
  const wage = rng('CFG_wage').getValues()
    .filter((r) => r[0] !== '' && r[0] !== null)
    .map((r) => ({ min: Number(r[0]), max: Number(r[1]), wage: Number(r[2]) }));
  return { quarter, ratees, items, wageTable: wage };
}

function doGet(e) {
  const action = e.parameter.action;
  if (action === 'config') {
    return jsonOut(readConfig());   // 不含通行碼
  }
  return jsonOut({ error: 'unknown action' });
}
```

- [ ] **Step 3: 部署並驗證 config**

部署為 Web App（新版本）。在瀏覽器開 `{WEBAPP_URL}?action=config`。
Expected: 回傳 JSON，含 `quarter`、`ratees`(6 名)、`items`(6 項，每項 5 個 levels)、`wageTable`。

- [ ] **Step 4: 把部署 URL 記到 README**

`README.md` 新增「Apps Script 部署 URL」「重新部署步驟（每次改 Code.gs 要建立新版本）」。

- [ ] **Step 5: Commit**

```bash
git add apps-script/Code.gs README.md
git commit -m "feat: Apps Script 骨架與 config 讀取"
```

---

### Task 9: `doPost` 寫入互評 ＋ 指紋防重

**Files:**
- Modify: `apps-script/Code.gs`

**Interfaces:**
- Consumes: `PeerSubmission` JSON（POST body）。
- Produces: 在 `評分紀錄` 為每位受評者寫一列；同季同指紋已存在 → 回 `{ ok: false, reason: 'duplicate' }`；成功 → `{ ok: true }`。

- [ ] **Step 1: 實作 doPost（peer 分支）**

加到 `Code.gs`：
```js
function alreadySubmitted(quarter, fingerprint) {
  const sh = ss().getSheetByName('評分紀錄');
  const values = sh.getDataRange().getValues(); // 含標題列
  for (let i = 1; i < values.length; i++) {
    if (values[i][1] === quarter && values[i][10] === fingerprint) return true;
  }
  return false;
}

function handlePeer(p) {
  if (alreadySubmitted(p.quarter, p.fingerprint)) {
    return { ok: false, reason: 'duplicate' };
  }
  const sh = ss().getSheetByName('評分紀錄');
  const now = new Date();
  p.ratings.forEach((r) => {
    sh.appendRow([now, p.quarter, r.ratee, ...r.scores, p.note || '', p.fingerprint]);
  });
  return { ok: true };
}

function doPost(e) {
  const lock = LockService.getScriptLock();
  lock.waitLock(20000);
  try {
    const p = JSON.parse(e.postData.contents);
    if (p.type === 'peer') return jsonOut(handlePeer(p));
    return jsonOut({ ok: false, reason: 'unknown type' });
  } finally {
    lock.releaseLock();
  }
}
```

> `LockService` 防止多人同時 append 互蓋。

- [ ] **Step 2: 重新部署（新版本）**

依 README 步驟建立新版本。

- [ ] **Step 3: 手動驗證寫入與防重**

用 curl（或 Apps Script 編輯器的測試）送一筆：
```bash
curl -L -X POST "{WEBAPP_URL}" -H 'Content-Type: application/json' \
  -d '{"type":"peer","quarter":"2026-Q1","fingerprint":"test123","note":"","ratings":[{"ratee":"許雅筑","scores":[5,5,4,4,4,4]}]}'
```
Expected: 第一次 `{"ok":true}`，`評分紀錄` 多一列；同 fingerprint 再送一次 → `{"ok":false,"reason":"duplicate"}`。

- [ ] **Step 4: Commit**

```bash
git add apps-script/Code.gs
git commit -m "feat: doPost 寫入互評並以指紋防重"
```

---

### Task 10: `doPost` 主管調整（通行碼）＋ `doGet?action=adminData`

**Files:**
- Modify: `apps-script/Code.gs`

**Interfaces:**
- Consumes: `AdjustSubmission`、`GET ?action=adminData&passcode=&quarter=`。
- Produces:
  - adjust：通行碼錯 → `{ ok: false, reason: 'unauthorized' }`；對 → 在 `主管調整` upsert（同季同受評者覆蓋）→ `{ ok: true }`。
  - adminData：通行碼錯 → `{ error: 'unauthorized' }`；對 → `AdminData`。

- [ ] **Step 1: 實作通行碼檢查、adjust upsert、adminData**

加到 `Code.gs`：
```js
function checkPass(pass) {
  return String(pass) === String(rng('CFG_passcode').getValue());
}

function handleAdjust(p) {
  if (!checkPass(p.passcode)) return { ok: false, reason: 'unauthorized' };
  const sh = ss().getSheetByName('主管調整');
  const values = sh.getDataRange().getValues();
  let rowIdx = -1;
  for (let i = 1; i < values.length; i++) {
    if (values[i][0] === p.quarter && values[i][1] === p.ratee) { rowIdx = i + 1; break; }
  }
  const row = [p.quarter, p.ratee, p.attitudeAdjust || 0, p.attitudeReason || '',
               p.competencyAdjust || 0, p.competencyReason || '', new Date(), '主管'];
  if (rowIdx === -1) sh.appendRow(row);
  else sh.getRange(rowIdx, 1, 1, row.length).setValues([row]);
  return { ok: true };
}

function readAdminData(passcode, quarter) {
  if (!checkPass(passcode)) return { error: 'unauthorized' };
  const config = readConfig();
  const recSh = ss().getSheetByName('評分紀錄').getDataRange().getValues();
  const peerRatings = [];
  for (let i = 1; i < recSh.length; i++) {
    if (recSh[i][1] === quarter) {
      peerRatings.push({ ratee: recSh[i][2], scores: recSh[i].slice(3, 9).map(Number) });
    }
  }
  const adjSh = ss().getSheetByName('主管調整').getDataRange().getValues();
  const adjustments = [];
  for (let i = 1; i < adjSh.length; i++) {
    if (adjSh[i][0] === quarter) {
      adjustments.push({
        ratee: adjSh[i][1],
        attitudeAdjust: Number(adjSh[i][2]) || 0, attitudeReason: adjSh[i][3] || '',
        competencyAdjust: Number(adjSh[i][4]) || 0, competencyReason: adjSh[i][5] || '',
      });
    }
  }
  return { config, peerRatings, adjustments };
}
```
在 `doGet` 加分支：
```js
  if (action === 'adminData') {
    return jsonOut(readAdminData(e.parameter.passcode, e.parameter.quarter));
  }
```
在 `doPost` 的 type 判斷加：
```js
    if (p.type === 'adjust') return jsonOut(handleAdjust(p));
```

- [ ] **Step 2: 重新部署（新版本）**

- [ ] **Step 3: 手動驗證**

- 錯誤通行碼 `GET ?action=adminData&passcode=wrong&quarter=2026-Q1` → `{"error":"unauthorized"}`。
- 正確通行碼 → 回 `config`＋`peerRatings`（含 Task 9 寫入那筆）＋`adjustments`(空)。
- 送 adjust（正確通行碼）→ `主管調整` 出現一列；再送同季同人 → 該列被覆蓋（不新增）。

- [ ] **Step 4: Commit**

```bash
git add apps-script/Code.gs
git commit -m "feat: 主管調整 upsert 與 adminData（通行碼保護）"
```

---

## Phase D — 前端畫面

> 前端以 preview 工具驗證（preview_start / preview_snapshot / preview_click / preview_fill / preview_console_logs）。需先設定 `js/config.js` 的 `APPS_SCRIPT_URL`。

### Task 11: 設定檔、API 封裝、共用樣式

**Files:**
- Create: `js/config.js`
- Create: `js/api.js`
- Create: `css/style.css`

**Interfaces:**
- Produces：
  - `js/config.js`：`export const APPS_SCRIPT_URL = '...';`
  - `api.js`：`fetchConfig(): Promise<Config>`、`submitPeer(payload): Promise<{ok,reason?}>`、`fetchAdminData(passcode, quarter): Promise<AdminData|{error}>`、`submitAdjust(payload): Promise<{ok,reason?}>`。

- [ ] **Step 1: 建立 config 與 api**

`js/config.js`：
```js
// 部署 Apps Script 後填入 Web App URL（/exec 結尾）
export const APPS_SCRIPT_URL = 'PASTE_WEBAPP_URL_HERE';
```

`js/api.js`：
```js
import { APPS_SCRIPT_URL } from './config.js';

async function postJSON(payload) {
  const res = await fetch(APPS_SCRIPT_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'text/plain;charset=utf-8' }, // 避開 CORS preflight
    body: JSON.stringify(payload),
  });
  if (!res.ok) throw new Error('network');
  return res.json();
}

export async function fetchConfig() {
  const res = await fetch(`${APPS_SCRIPT_URL}?action=config`);
  if (!res.ok) throw new Error('network');
  return res.json();
}
export function submitPeer(payload) { return postJSON(payload); }
export function submitAdjust(payload) { return postJSON(payload); }
export async function fetchAdminData(passcode, quarter) {
  const url = `${APPS_SCRIPT_URL}?action=adminData&passcode=${encodeURIComponent(passcode)}&quarter=${encodeURIComponent(quarter)}`;
  const res = await fetch(url);
  if (!res.ok) throw new Error('network');
  return res.json();
}
```

> 註：用 `text/plain` 送 JSON 以避免瀏覽器對 Apps Script 觸發 CORS preflight；`Code.gs` 已用 `JSON.parse(e.postData.contents)` 解析，相容。

- [ ] **Step 2: 建立基本樣式**

`css/style.css`：
```css
:root { --brand:#c0392b; --bg:#fff7f5; --ink:#333; --line:#e6d6d2; }
* { box-sizing: border-box; }
body { margin:0; font-family: system-ui, "PingFang TC", sans-serif; color:var(--ink); background:var(--bg); }
.wrap { max-width:760px; margin:0 auto; padding:16px; }
h1 { color:var(--brand); font-size:20px; }
.card { background:#fff; border:1px solid var(--line); border-radius:12px; padding:14px; margin:12px 0; }
.stars { display:flex; gap:6px; font-size:26px; cursor:pointer; }
.stars span { color:#ddd; }
.stars span.on { color:#f1c40f; }
button { background:var(--brand); color:#fff; border:0; border-radius:8px; padding:10px 16px; font-size:16px; }
button:disabled { opacity:.5; }
.msg { padding:10px; border-radius:8px; }
.msg.ok { background:#eafaf1; color:#1e8449; }
.msg.err { background:#fdecea; color:#c0392b; }
table { width:100%; border-collapse:collapse; }
th, td { border:1px solid var(--line); padding:6px; font-size:14px; text-align:center; }
.muted { color:#999; }
```

- [ ] **Step 3: Commit**

```bash
git add js/config.js js/api.js css/style.css
git commit -m "feat: 前端設定、API 封裝與共用樣式"
```

---

### Task 12: 同仁互評頁 `index.html` ＋ `eval-page.js`

**Files:**
- Create: `index.html`
- Create: `js/eval-page.js`

**Interfaces:**
- Consumes: `fetchConfig`, `submitPeer`（api.js）、`validatePeerSubmission`（validate.js）、`deviceFingerprint`（fingerprint.js）。

- [ ] **Step 1: 建立 index.html**

```html
<!doctype html>
<html lang="zh-Hant">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>計時同仁評鑑</title>
  <link rel="stylesheet" href="css/style.css" />
</head>
<body>
  <div class="wrap">
    <h1 id="title">計時同仁評鑑</h1>
    <p>誠實面對、客觀檢視。每項點選星等，星等說明可展開參考。匿名填寫，謝謝你的回饋 ⭐️</p>
    <div id="forms"></div>
    <div class="card">
      <label>整體備註（選填）<br /><textarea id="note" rows="2" style="width:100%"></textarea></label>
    </div>
    <div id="errors" class="msg err" style="display:none"></div>
    <button id="submit">送出評鑑</button>
    <div id="result" class="msg" style="display:none"></div>
  </div>
  <script type="module" src="js/eval-page.js"></script>
</body>
</html>
```

- [ ] **Step 2: 建立 eval-page.js**

```js
import { fetchConfig, submitPeer } from './api.js';
import { validatePeerSubmission } from './validate.js';
import { deviceFingerprint } from './fingerprint.js';

const state = { config: null, ratings: new Map() }; // ratee -> number[6]

function storageKey(q) { return `peer_done_${q}`; }

function renderStars(ratee, item, idx) {
  const wrap = document.createElement('div');
  const title = document.createElement('div');
  title.textContent = `${idx + 1}. ${item.label}`;
  const stars = document.createElement('div');
  stars.className = 'stars';
  for (let v = 1; v <= 5; v++) {
    const s = document.createElement('span');
    s.textContent = '★';
    s.onclick = () => {
      const arr = state.ratings.get(ratee);
      arr[idx] = v;
      [...stars.children].forEach((el, i) => el.classList.toggle('on', i < v));
    };
    stars.appendChild(s);
  }
  const help = document.createElement('details');
  help.innerHTML = `<summary class="muted">星等說明</summary>` +
    item.levels.map((lv, i) => `<div>${5 - i}★ ${lv}</div>`).join('');
  wrap.append(title, stars, help);
  return wrap;
}

function render() {
  document.getElementById('title').textContent = `計時同仁評鑑（${state.config.quarter}）`;
  const host = document.getElementById('forms');
  host.innerHTML = '';
  state.config.ratees.forEach((ratee) => {
    state.ratings.set(ratee, new Array(state.config.items.length).fill(0));
    const card = document.createElement('div');
    card.className = 'card';
    const h = document.createElement('h3');
    h.textContent = ratee;
    card.appendChild(h);
    state.config.items.forEach((item, idx) => card.appendChild(renderStars(ratee, item, idx)));
    host.appendChild(card);
  });
}

async function init() {
  if (localStorage.getItem(storageKey('pending'))) { /* 暫存還原見下 */ }
  try {
    state.config = await fetchConfig();
  } catch {
    document.getElementById('result').style.display = 'block';
    document.getElementById('result').className = 'msg err';
    document.getElementById('result').textContent = '載入失敗，請重新整理';
    return;
  }
  if (localStorage.getItem(storageKey(state.config.quarter))) {
    document.querySelector('.wrap').innerHTML =
      '<div class="msg ok">這個裝置本季已完成評鑑，謝謝你！</div>';
    return;
  }
  render();
}

document.getElementById('submit').onclick = async () => {
  const ratings = state.config.ratees.map((r) => ({ ratee: r, scores: state.ratings.get(r) }));
  const errs = validatePeerSubmission(ratings, state.config.ratees, state.config.items.length);
  const errBox = document.getElementById('errors');
  if (errs.length) {
    errBox.style.display = 'block';
    errBox.innerHTML = errs.join('<br>');
    return;
  }
  errBox.style.display = 'none';
  const btn = document.getElementById('submit');
  btn.disabled = true;
  const payload = {
    type: 'peer', quarter: state.config.quarter,
    fingerprint: deviceFingerprint(), note: document.getElementById('note').value, ratings,
  };
  const box = document.getElementById('result');
  box.style.display = 'block';
  try {
    const res = await submitPeer(payload);
    if (res.ok) {
      localStorage.setItem(storageKey(state.config.quarter), '1');
      box.className = 'msg ok';
      box.textContent = '已完成，謝謝你的回饋！';
      document.getElementById('forms').style.display = 'none';
    } else if (res.reason === 'duplicate') {
      box.className = 'msg ok';
      box.textContent = '本裝置本季已填寫過，謝謝！';
    } else {
      throw new Error('rejected');
    }
  } catch {
    box.className = 'msg err';
    box.textContent = '送出失敗，請稍後再試一次';
    btn.disabled = false;
  }
};

init();
```

- [ ] **Step 3: 用 preview 啟動並驗證**

設定 `js/config.js` 的 URL 後：preview_start（靜態伺服器指向專案根）。
- preview_snapshot：應看到標題含季度、6 位同仁各 6 項星等。
- preview_click 某顆星 → 該列前 N 顆變亮（preview_snapshot 確認 class `on`）。
- 未填完按送出 → 出現錯誤清單（preview_snapshot）。
- preview_console_logs：無未捕捉錯誤。

- [ ] **Step 4: 驗證送出成功流程**

填妥全部 → 送出 → 出現「已完成，謝謝你的回饋！」；`評分紀錄` 多出對應列；重新整理頁面 → 顯示「本季已完成」（localStorage 生效）。preview_screenshot 留證。

- [ ] **Step 5: Commit**

```bash
git add index.html js/eval-page.js
git commit -m "feat: 同仁互評頁（動態題目、星等、驗證、防重、送出）"
```

---

### Task 13: 管理頁 `admin.html` ＋ `admin-page.js`（通行碼、結果總覽、主管調整、明細、進度）

**Files:**
- Create: `admin.html`
- Create: `js/admin-page.js`

**Interfaces:**
- Consumes: `fetchAdminData`, `submitAdjust`（api.js）、`aggregateRatee`, `round1`（scoring.js）。

- [ ] **Step 1: 建立 admin.html**

```html
<!doctype html>
<html lang="zh-Hant">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>評鑑管理</title>
  <link rel="stylesheet" href="css/style.css" />
</head>
<body>
  <div class="wrap">
    <h1>評鑑管理</h1>
    <div id="gate" class="card">
      <label>通行碼 <input id="pass" type="password" /></label>
      <button id="enter">進入</button>
      <div id="gateErr" class="msg err" style="display:none">通行碼錯誤</div>
    </div>
    <div id="panel" style="display:none">
      <div id="progress" class="card"></div>
      <div id="overview" class="card"></div>
      <div id="detail" class="card"></div>
    </div>
  </div>
  <script type="module" src="js/admin-page.js"></script>
</body>
</html>
```

- [ ] **Step 2: 建立 admin-page.js**

```js
import { fetchAdminData, submitAdjust } from './api.js';
import { aggregateRatee, round1 } from './scoring.js';

let DATA = null, PASS = '';

function groupByRatee(peerRatings, ratees) {
  const map = new Map(ratees.map((r) => [r, []]));
  peerRatings.forEach((p) => { if (map.has(p.ratee)) map.get(p.ratee).push(p.scores); });
  return map;
}

function results() {
  const { config, peerRatings, adjustments } = DATA;
  const grouped = groupByRatee(peerRatings, config.ratees);
  const adjBy = new Map(adjustments.map((a) => [a.ratee, a]));
  return config.ratees.map((ratee) =>
    aggregateRatee({
      ratee,
      scoresList: grouped.get(ratee),
      adjustment: adjBy.get(ratee) || {},
      wageTable: config.wageTable,
    })
  );
}

function wageText(w) {
  if (w.status === 'ok') return `$${w.wage}`;
  if (w.status === 'insufficient') return '資料不足';
  return '需人工確認';
}
function numText(n) { return n === null ? '—' : round1(n); }

function renderProgress(rows) {
  const total = rows.reduce((a, r) => a + r.responseCount, 0);
  document.getElementById('progress').innerHTML =
    `<b>填寫進度</b>：本季共收到 ${total} 筆互評（${DATA.config.quarter}）`;
}

function renderOverview(rows) {
  const head = '<tr><th>同仁</th><th>態度分</th><th>態度調整</th><th>職能分</th><th>職能調整</th><th>最終總分</th><th>時薪</th><th>互評份數</th></tr>';
  const body = rows.map((r) => `<tr>
    <td><a href="#" data-r="${r.ratee}">${r.ratee}</a></td>
    <td>${numText(r.attitude)}</td><td>${r.attitudeAdjust}</td>
    <td>${r.competencyCounted ? numText(r.competency) : '未計'}</td><td>${r.competencyAdjust}</td>
    <td>${numText(r.subtotal)}</td><td>${wageText(r.wage)}</td><td>${r.responseCount}</td>
  </tr>`).join('');
  const host = document.getElementById('overview');
  host.innerHTML = `<b>總覽</b><table>${head}${body}</table>`;
  host.querySelectorAll('a[data-r]').forEach((a) =>
    a.onclick = (e) => { e.preventDefault(); renderDetail(a.dataset.r); });
}

function renderDetail(ratee) {
  const { config } = DATA;
  const row = results().find((r) => r.ratee === ratee);
  const adj = DATA.adjustments.find((a) => a.ratee === ratee) || {};
  const items = config.items.map((it, i) =>
    `<tr><td>${it.label}</td><td>${row.itemAverages ? round1(row.itemAverages[i]) : '—'}</td></tr>`).join('');
  document.getElementById('detail').innerHTML = `
    <b>${ratee} 明細</b>
    <table><tr><th>項目</th><th>互評平均</th></tr>${items}</table>
    <div class="card">
      <div>主管調整</div>
      態度 ± <input id="aAdj" type="number" value="${adj.attitudeAdjust || 0}" style="width:70px">
      原因 <input id="aRsn" value="${adj.attitudeReason || ''}"><br>
      職能 ± <input id="cAdj" type="number" value="${adj.competencyAdjust || 0}" style="width:70px">
      原因 <input id="cRsn" value="${adj.competencyReason || ''}"><br>
      <button id="saveAdj">儲存調整</button>
      <span id="adjMsg"></span>
    </div>`;
  document.getElementById('saveAdj').onclick = async () => {
    const payload = {
      type: 'adjust', passcode: PASS, quarter: config.quarter, ratee,
      attitudeAdjust: Number(document.getElementById('aAdj').value) || 0,
      attitudeReason: document.getElementById('aRsn').value,
      competencyAdjust: Number(document.getElementById('cAdj').value) || 0,
      competencyReason: document.getElementById('cRsn').value,
    };
    const msg = document.getElementById('adjMsg');
    try {
      const res = await submitAdjust(payload);
      if (!res.ok) throw new Error();
      msg.textContent = '已儲存';
      await load();              // 重新抓資料並重繪總覽
      renderDetail(ratee);
    } catch { msg.textContent = '儲存失敗，請重試'; }
  };
}

async function load() {
  DATA = await fetchAdminData(PASS, document.querySelector('#quarterHidden')?.value || CURRENT_Q);
  const rows = results();
  renderProgress(rows);
  renderOverview(rows);
}

let CURRENT_Q = '';
document.getElementById('enter').onclick = async () => {
  PASS = document.getElementById('pass').value;
  // 先用 config 拿當前季度
  const cfg = await fetch(`${(await import('./config.js')).APPS_SCRIPT_URL}?action=config`).then((r) => r.json());
  CURRENT_Q = cfg.quarter;
  const data = await fetchAdminData(PASS, CURRENT_Q);
  if (data.error) { document.getElementById('gateErr').style.display = 'block'; return; }
  DATA = data;
  document.getElementById('gate').style.display = 'none';
  document.getElementById('panel').style.display = 'block';
  const rows = results();
  renderProgress(rows);
  renderOverview(rows);
};
```

- [ ] **Step 3: 用 preview 驗證通行碼與總覽**

- 錯誤通行碼 → 顯示「通行碼錯誤」（preview_fill + preview_click + preview_snapshot）。
- 正確通行碼 → 顯示進度、總覽表（每位同仁態度分/時薪/份數）；職能欄顯示「未計」。

- [ ] **Step 4: 驗證主管調整流程**

點一位同仁 → 明細顯示 6 項互評平均 → 填態度 ±2、原因 → 儲存 → 「已儲存」，總覽該人態度調整與最終總分更新；`主管調整` 分頁出現/更新該列。preview_screenshot 留證。

- [ ] **Step 5: Commit**

```bash
git add admin.html js/admin-page.js
git commit -m "feat: 管理頁（通行碼、結果總覽、主管調整、明細、進度）"
```

---

### Task 14: README 部署文件、回填初始資料、端到端驗收

**Files:**
- Modify: `README.md`

**Interfaces:** 無新程式；整合驗收。

- [ ] **Step 1: 補完 README**

內容含：
1. Google Sheet 分頁與命名範圍建立步驟（對照設計 §3）。
2. Apps Script 部署步驟（貼 `Code.gs`、部署 Web App、權限、取得 `/exec` URL、每次改碼要新版本）。
3. 前端設定：把 URL 填進 `js/config.js`。
4. GitHub Pages 發布步驟。
5. 待回填資料：6 題行為描述（沿用現有表單文字）、時薪對照表、通行碼初值、第二階段職能 70 分。
6. 測試指令：`node --test`。

- [ ] **Step 2: 回填初始資料到 Sheet**

把現有表單 6 題的 5→1 星描述填入 `設定/項目`；填入時薪對照表與通行碼；設定 `季度=2026-Q1`、6 位受評同仁。

- [ ] **Step 3: 端到端驗收（對照設計文件逐項）**

- 互評頁：匿名填 2～3 份（不同指紋）→ `評分紀錄` 累積。
- 管理頁：總覽態度分 = 互評平均加總、時薪查表正確；資料不足者顯示「資料不足」。
- 主管調整：±值反映到最終總分；表外總分顯示「需人工確認」。
- 防重：同指紋重送被擋；同瀏覽器重整顯示已填。
- `node --test` 全綠。

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: 部署與設定說明、端到端驗收"
```

---

## 自我審查結果（對照設計文件）

- **§2 架構**：Task 8–14 覆蓋（前端＋Apps Script＋Sheet）。計分單一來源改由 `scoring.js` 擔任、Apps Script 不重算——較設計 §7「彙總由 Apps Script 算」更能保證「只有一份計分邏輯」，已於 Architecture 與 Global Constraints 註明此調整。
- **§3 資料模型**：Task 8 建立四分頁；欄位與設計一致。
- **§4 計分與邊界**：Task 1–5（平均、加總、調整、職能留空、查表、資料不足、表外）。
- **§5 畫面**：互評頁 Task 12、主管頁＋結果頁＋進度 Task 13；設定走 Sheet（Task 14 README）。
- **§6 防重**：localStorage（Task 12）＋指紋（Task 7 產生、Task 9 後端比對）。
- **§7 錯誤處理**：送出失敗提示（Task 12/13）、通行碼後端比對（Task 10）、前端只顯示。
- **§8 測試**：Phase A/B 全 TDD；Apps Script 手動驗證；前端 preview 驗證。
- **§9 職能擴充**：型別與 `結果`/`finalSubtotal` 已預留 competency 欄位與 `competencyCounted`。
- **型別一致性**：`Config`/`PeerSubmission`/`AdjustSubmission`/`AdminData`/`RateeResult` 全程一致；函式名 `averageItemScores`/`attitudeScore`/`finalSubtotal`/`lookupWage`/`aggregateRatee`/`round1`/`validatePeerSubmission`/`deviceFingerprint` 前後一致。
